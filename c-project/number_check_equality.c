//This is a C Program which calculates the number of integers divisible by 5 in the given range.
#include <stdio.h>

int  main(){
     int y,x;
     printf("Enter y & x value: ");
     scanf("%i%i",&y,&x);
     if(x==y)
           printf("\n %i & %i are equal",x,y);
     else
          printf("\n %i & %i are not equal",x,y);
     return 0;
}
